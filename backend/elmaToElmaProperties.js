// backend/elmaToElmaProperties.js

const { makeReq, logWriter, generateCSV, generateAndMakeRequest, createDataFile, uploadFile, importData } = require('./common');

// Здесь будут функции, специфичные для работы с свойствами

// Экспорт функций
module.exports = {
    // Добавьте здесь функции для работы с свойствами
};
